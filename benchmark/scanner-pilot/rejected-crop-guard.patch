diff --git a/scanner-v16-core.js b/scanner-v16-core.js
index 39a09b7..e96bc86 100644
--- a/scanner-v16-core.js
+++ b/scanner-v16-core.js
@@ -145,7 +145,9 @@
       const s=size(source);
       // A tightly framed card already has its footer at the image edge. Do not
       // shave off 2.5% and discard secret-rare / One Piece codes at the bottom.
-      if(Math.abs(s.w/s.h-CARD_RATIO)<.035)Object.assign(regions[0],{x:0,y:0,w:s.w,h:s.h});
+      // Relative tolerance: a normal 3:4 phone photo is 4.76% wider than a
+      // trading card. It must reach contour detection, even when portrait.
+      if(Math.abs(s.w/s.h/CARD_RATIO-1)<.02)Object.assign(regions[0],{x:0,y:0,w:s.w,h:s.h});
       else{const detected=detectedRegion(source);if(detected)Object.assign(regions[0],detected)}
     }
     for(let i=0;i<regions.length;i++){
diff --git a/tests/scanner-v16-acceptance-regression-test.mjs b/tests/scanner-v16-acceptance-regression-test.mjs
index 4d563bd..f50b25f 100644
--- a/tests/scanner-v16-acceptance-regression-test.mjs
+++ b/tests/scanner-v16-acceptance-regression-test.mjs
@@ -66,6 +66,26 @@ globalThis.selectedScanTcg='one_piece';
 globalThis.catalogLookup=async(identifier,context)=>{contexts.push(context.tcg);return context.tcg==='pokemon'?client.lookup(identifier,context):op.lookup(identifier,context)};
 await import('../scanner-v16-core.js');
 const core=globalThis.DV_SCAN_V16_CORE;resilience.install();
+// Ordinary iPhone portrait photos (3:4) must not bypass the contour detector.
+// Record the actual pixels sent to preparation; exact card-sized inputs and
+// explicit live captures retain their original edges and footer.
+const actualCreate=globalThis.document.createElement,actualLive=globalThis.DV_SCAN_V16_LIVE,draws=[];
+globalThis.document.createElement=()=>({width:1,height:1,getContext:()=>({
+  drawImage(...args){draws.push(args)},fillRect(){},getImageData(_x,_y,w,h){return{data:new Uint8ClampedArray(w*h*4),width:w,height:h}}
+})});
+let contourCalls=0;
+globalThis.DV_SCAN_V16_LIVE={inspect(){contourCalls++;return{presence:true,aligned:true,rect:{x:30,y:40,w:200,h:280}}},captureRect(){return{x:120,y:160,w:800,h:1120}}};
+const beforeSourceTest=globalThis.Tesseract;globalThis.Tesseract=null;
+await core.analyze({width:1152,height:1536},{tcg:'pokemon'});
+assert.equal(contourCalls,1,'3:4 iPhone photo must enter contour detection');
+assert.ok(draws.some(a=>a[1]===120&&a[2]===160&&a[3]===800&&a[4]===1120),'analysis must crop the detected card, not the complete 3:4 photo');
+draws.length=0;
+await core.analyze({width:630,height:880},{tcg:'pokemon'});
+assert.equal(contourCalls,1,'tight 63:88 reference must retain its footer');
+assert.ok(draws.some(a=>a[1]===0&&a[2]===0&&a[3]===630&&a[4]===880));
+await core.analyze({width:1152,height:1536},{tcg:'pokemon',sourcePrepared:true});
+assert.equal(contourCalls,1,'explicitly prepared capture must not be cropped twice');
+globalThis.document.createElement=actualCreate;globalThis.DV_SCAN_V16_LIVE=actualLive;globalThis.Tesseract=beforeSourceTest;
 let pack=await core.analyze({width:630,height:880},{tcg:'pokemon'});
 assert.equal(pack.results[0].id.code,'074/084');assert.equal(pack.results[0].best.name,'Retourorden');assert.equal(pack.results[0].tcg,'pokemon');
 assert.equal(globalThis.selectedScanTcg,'one_piece','core must never mutate the global TCG selection');
