(()=>{const page=(location.pathname.split('/').pop()||'app.html').toLowerCase();const items=[['app.html','HOME'],['collect.html','COLLECT'],['trade.html','TRADE'],['battle.html','BATTLE']];function mount(){const header=document.querySelector('header.top');if(!header||header.querySelector('.dv-global-nav'))return;const nav=document.createElement('nav');nav.className='dv-global-nav';nav.setAttribute('aria-label','DUELVANTA Seitenübersicht');nav.innerHTML=`<button class="dv-nav-toggle" type="button" aria-expanded="false">MENÜ <span>☰</span></button><div class="dv-nav-links">${items.map(([href,label])=>`<a href="${href}" class="${page===href?'active':''}">${label}</a>`).join('')}<a href="staff-apply.html" class="${page==='staff-apply.html'?'active':''}">STAFF</a></div>`;const brand=header.querySelector('.brand');brand?.after(nav);nav.querySelector('.dv-nav-toggle').onclick=e=>{const open=nav.classList.toggle('open');e.currentTarget.setAttribute('aria-expanded',String(open))};document.addEventListener('click',e=>{if(!nav.contains(e.target))nav.classList.remove('open')})}document.readyState==='loading'?document.addEventListener('DOMContentLoaded',mount):mount()})();