(()=>{const page=(location.pathname.split('/').pop()||'app.html').toLowerCase();const items=[['app.html','HOME'],['collect.html','COLLECT'],['trade.html','TRADE'],['battle.html','BATTLE']];async function isOwner(){try{if(!window.supabase)return false;const db=window.__dvNavDb||(window.__dvNavDb=window.supabase.createClient('https://enifiaqsnqtbzylnfrpi.supabase.co','sb_publishable_pk2szDe_g7fJLUdAMEUevw_odrDmnuM',{auth:{persistSession:true,autoRefreshToken:true}}));const{data:{session}}=await db.auth.getSession();if(!session)return false;const{data}=await db.from('profiles').select('role').eq('id',session.user.id).single();return data?.role==='owner'}catch{return false}}async function mount(){const header=document.querySelector('header.top');if(!header||header.querySelector('.dv-global-nav'))return;const owner=await isOwner();const nav=document.createElement('nav');nav.className='dv-global-nav';nav.setAttribute('aria-label','DUELVANTA Seitenübersicht');nav.innerHTML=`<button class="dv-nav-toggle" type="button" aria-expanded="false">MENÜ <span>☰</span></button><div class="dv-nav-links">${items.map(([href,label])=>`<a href="${href}" class="${page===href?'active':''}">${label}</a>`).join('')}<a href="staff-apply.html" class="${page==='staff-apply.html'?'active':''}">STAFF</a>${owner?`<a href="control-center.html" class="${page==='control-center.html'?'active':''}">CONTROL</a>`:''}</div>`;const brand=header.querySelector('.brand');brand?.after(nav);nav.querySelector('.dv-nav-toggle').onclick=e=>{const open=nav.classList.toggle('open');e.currentTarget.setAttribute('aria-expanded',String(open))};document.addEventListener('click',e=>{if(!nav.contains(e.target))nav.classList.remove('open')})}document.readyState==='loading'?document.addEventListener('DOMContentLoaded',mount):mount()})();